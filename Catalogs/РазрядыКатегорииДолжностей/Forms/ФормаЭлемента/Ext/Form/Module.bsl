// 4D:ERP для Беларуси
// {
// Форма изменена
// }
// 4D


#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ЭтаФорма.Заголовок = РазрядыКатегорииДолжностей.ИнициализироватьЗаголовокФормыИРеквизитов("СправочникРазрядовЭлемент");
КонецПроцедуры

// 4D:ERP для Беларуси, Яна, 08.08.2017 11:18:14 
// Справочник "Тарифные группы", №15674 
// {
&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	
	Если Объект.ПовышающийКоэффициент = 0 Тогда
		Объект.ПовышающийКоэффициент = 1;
	КонецЕсли;
		
КонецПроцедуры

&НаСервере
Процедура РазрядЕТСПриИзмененииНаСервере()
	Объект.Коэффициент = Объект.РазрядЕТС.Коэффициент;	
КонецПроцедуры

&НаКлиенте
Процедура РазрядЕТСПриИзменении(Элемент)
	РазрядЕТСПриИзмененииНаСервере();
КонецПроцедуры
// }
// 4D


#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура НаименованиеПриИзменении(Элемент)
	
	Если НЕ ЗначениеЗаполнено(Объект.НаименованиеПолное) Тогда
		Объект.НаименованиеПолное = Объект.Наименование;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
